{
  "version": 2,
  "framework": "nextjs",
  "buildCommand": "cd frontend && (if exist pages rd /s /q pages) && npm install && npm run vercel-build",
  "outputDirectory": "frontend/.next",
  "builds": [
    {
      "src": "frontend/next.config.js",
      "use": "@vercel/next"
    }
  ]
} 