{
  "version": 2,
  "framework": "nextjs",
  "buildCommand": "cd frontend && rm -rf pages && rm -rf pages/_app.tsx && rm -rf pages/_document.tsx && rm -rf pages/index.tsx && npm install && npm run vercel-build",
  "outputDirectory": "frontend/.next",
  "builds": [
    {
      "src": "frontend/next.config.js",
      "use": "@vercel/next"
    }
  ]
} 