{
  "version": 2,
  "framework": "nextjs",
  "buildCommand": "cd frontend && find . -type d -name pages -exec rm -rf {} + && npm install && npm run vercel-build",
  "outputDirectory": "frontend/.next",
  "builds": [
    {
      "src": "frontend/next.config.js",
      "use": "@vercel/next"
    }
  ]
} 